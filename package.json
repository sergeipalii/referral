{
  "name": "referral-system",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm -w @referral-system/backend run start:dev",
    "dev:frontend": "npm -w @referral-system/frontend run dev",
    "build": "npm -w @referral-system/backend run build && npm -w @referral-system/frontend run build",
    "lint": "npm run lint --workspaces --if-present",
    "format": "prettier --write \"apps/**/*.{ts,tsx,js,jsx,json}\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.4.2"
  }
}
